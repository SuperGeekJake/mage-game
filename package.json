{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "test": "lerna run test",
    "watch": "lerna run watch --parallel"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
